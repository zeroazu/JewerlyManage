# Seller DB - jewerly.db 

## 使用方式

1. 解壓縮專案
2. 把你的 `jewerly.db` 放到跟 `app.py` 同一層
3. 執行：

```powershell
python -m pip install -r requirements.txt
python app.py
```

4. 開啟：

```text
http://127.0.0.1:5000
```

## 

